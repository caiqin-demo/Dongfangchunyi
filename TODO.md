# Landing Page 测评遗留 TODO

本文档只记录历次第三方测评中尚未完成、需要产品经理确认或需要进一步讨论的事项。测评意见是待验证的输入，不是自动修改授权；实施前仍须遵守 `AGENTS.md` 的验证期变更控制规则。

已完成的修复不重复列入。已由产品明确确认保留的设计也不列为缺陷，例如三张产品卡片继续使用相同产品图。

## Hero

### [ ] H-01 确认 Hero 高度与首屏构图目标

- 状态：需要产品经理讨论。
- 测评意见：当前 Hero 基准高度为 `474px`，测评建议参考更高的美式营销站首屏。
- 当前判断：不存在适用于所有美国网站的 `600–800px` 或 `85vh` 强制标准；是否增高属于品牌构图与首屏信息密度决定，不应仅凭行业概括修改。
- 需要确认：桌面、窄桌面和移动端的批准参考图；首屏是否需要露出下一节；Hero 图片的焦点和安全裁切区域。
- 完成条件：产品经理批准各关键视口的目标构图，并在所有支持语言下验证标题、说明和 CTA 无裁切、重叠或横向溢出。

### [ ] H-02 建立 CTA 转化目标和验证方式

- 状态：需要产品经理与数据负责人讨论。
- 已完成：CTA 的最小高度、字号、宽度、键盘访问和目标锚点已经修复并验证。
- 未完成：不能仅凭视觉测评证明 CTA “不转化”。
- 需要确认：主要转化目标、CTA 文案、最终目标页面或表单、事件名称、基准转化率，以及是否进行 A/B 测试。
- 完成条件：CTA 文案和去向得到产品批准；如要求验证转化效果，必须有可审查的分析事件和实验口径。

## Visual system

### [ ] V-01 在 Sprint 2 实现六张卡片底部操作条

- 目标 Sprint：Sprint 2。
- 状态：功能已规划，当前视觉设计必须保留。
- 当前情况：产品和服务卡片底部的 `+` 操作条是后续功能入口，不是待删除的假按钮。
- 需要确认：整张卡片还是操作条可点击；目标详情页、弹层或展开内容；每种语言的可访问名称；打开后的返回和焦点行为。
- 完成条件：鼠标、键盘和辅助技术均可操作；存在清晰的 hover、focus 和 active 状态；无障碍名称描述真实动作；不再使用纯装饰语义。

### [ ] V-02 评估是否引入独立拉丁展示字体

- 状态：低优先级设计讨论。
- 测评意见：`ABOUT US`、`Yeast Two Hybrid` 等拉丁文字目前使用 CJK 字体的拉丁字形，品牌个性较弱。
- 需要确认：品牌是否需要独立拉丁字体；批准字体、字重、许可和使用范围；增加字体后的性能预算。
- 完成条件：只有在产品经理批准明确的排版目标后实施；验证所有支持语言的字体回退、网络请求和布局偏移。

### [ ] V-03 复核核心卡片右上角装饰的可见性

- 状态：需要视觉确认。
- 测评意见：`.core-card::before` 与卡片背景亮度接近，装饰可能难以察觉。
- 需要确认：该装饰是否属于已批准设计；应提高对比度、保持弱化效果，还是移除。
- 完成条件：基于批准的设计基准决定，不得仅凭测评自动删除；在常用显示亮度和中、深色显示环境下人工验收。

### [ ] V-04 决定是否需要页面进入或滚动动效

- 状态：低优先级产品讨论。
- 当前判断：“没有动效”不是缺陷，不能为了显得专业强制添加动画。
- 需要确认：动效需要表达的状态、层级或信息关系；允许的持续时间和性能预算。
- 完成条件：只有存在明确交互目的时实施；所有非必要动效必须尊重 `prefers-reduced-motion`，并避免影响阅读、性能和截图回归稳定性。

## Layout

### [ ] L-01 确认全宽区块之间的分隔方式

- 状态：需要产品经理讨论。
- 测评意见：当前 `0.5rem` 面板间距可能被理解为渲染接缝。
- 需要确认：采用无缝全宽区块，还是明确的卡片式分区；若使用卡片式分区，需要批准外边距、圆角、背景和阴影关系。
- 完成条件：桌面与移动端使用同一可解释的分区逻辑，不出现像意外白缝或断层的视觉效果。

### [ ] L-02 逐步收敛剩余任意尺寸值

- 状态：技术债，需要视觉基准后实施。
- 已完成：主要容器、面板最小高度和部分布局值已经抽取为共享 token 或复用类。
- 未完成：页面仍有若干用于复刻已批准设计的任意字号、间距和尺寸值。
- 需要确认：哪些像素值属于必须保持的设计基准，哪些可以映射到统一的 4px/8px 间距阶梯。
- 完成条件：优先使用语义 token 和 Tailwind 尺度；任何会改变已批准视觉的收敛必须经过截图对比和产品审查。

### [ ] L-03 评估移动优先断点迁移

- 状态：技术债，需要单独实施计划。
- 测评意见：当前布局仍主要使用 `max-[960px]`、`max-[799px]` 和 `max-[640px]`，不是完整的移动优先标准断点体系。
- 风险区间：641–959px 的平板和窄桌面视口需要持续重点验证。
- 完成条件：若迁移，必须先固定现有视觉基准；覆盖每个断点两侧及所有支持语言，不得为追求标准类名而引入回归。

### [ ] L-04 决定移动导航的长期模式

- 状态：需要产品经理与可访问性讨论。
- 当前情况：移动端仍显示五个横向导航项；文字变长或新增语言时可能拥挤，点击目标和换行风险会增加。
- 需要确认：继续保留横向导航，还是采用菜单按钮；若采用菜单，需确定抽屉、弹层或下拉形式。
- 完成条件：所有支持语言在最窄目标视口可用；点击目标、键盘、焦点管理、Escape 和可访问名称通过验收。若实现交互式移动菜单，按 `AGENTS.md` 优先使用 Headless UI。

## Dead code from a previous life

### [ ] D-01 在 Sprint 2 清理 `card-action` 的临时状态

- 目标 Sprint：Sprint 2。
- 状态：等待 V-01 卡片功能实现。
- 已完成：旧版无样式支持的语义 class 已清理；`.core-card` 是实际 CSS 选择器。
- 未完成：`card-action` 当前保留用于计划中的卡片操作条，但尚无真实交互。
- 完成条件：Sprint 2 将其转为正式交互组件或明确的测试钩子，并补充注释/测试；不得在功能完成前误删。

## Technology

### [ ] T-01 澄清并修复默认生产构建门槛

- 状态：高优先级工程前置事项。
- 当前情况：`npm run build` 使用 Turbopack 时在受限环境出现 PostCSS/Tailwind 内部进程绑定端口 `Operation not permitted`；`next build --webpack` 可以通过。
- 需要确认：该问题是否只存在于本地沙箱，还是会在正式 CI/Vercel 环境复现；项目是否继续以默认 Turbopack 构建作为必过门槛。
- 完成条件：默认 `npm run build` 在正式验收环境稳定通过，或由团队批准、记录并在 CI 中采用明确的替代生产构建命令。

### Metadata is nearly empty — Sprint 5

以下事项统一留到 Sprint 5。现有的本地化 title、description、`metadataBase`、canonical、`hreflang`、`x-default` 和多语言 sitemap 已完成，不重复列入。

#### [ ] T-02 添加 Open Graph 与 Twitter 分享元数据

- 需要资料：每种语言的分享标题、摘要、正式 URL，以及批准的分享图；建议分享图为 1200×630。
- 需要讨论：所有语言共用一张无文字图片，还是每种语言使用独立图片；不同平台允许的裁切安全区域。
- 技术位置：各语言页面的 Next.js metadata，最终输出到 HTML `<head>`。
- 完成条件：Slack、LinkedIn、LINE 等目标平台或其调试工具能读取正确的标题、摘要、URL 和图片；平台缓存与裁切差异需记录为外部限制。

#### [ ] T-03 添加 `robots.ts`

- 需要资料：允许抓取的公开路径、需要禁止抓取的路径，以及正式 sitemap URL。
- 建议初始策略：允许抓取公开站点并声明 `https://www.easternpurity.com/sitemap.xml`。
- 技术位置：`src/app/robots.ts`，生成 `/robots.txt`。
- 完成条件：生产环境返回有效 robots.txt；不误阻止语言页面、字体、图片和 sitemap；规则经过产品与部署审查。

#### [ ] T-04 添加 Organization JSON-LD

- 需要资料：法定公司名称、各语言展示名称、正式网站、批准的 Logo URL、公开电话/邮箱/地址、官方社交账号。
- 需要确认：所有字段是否真实、可公开，并与页面可见内容保持一致。
- 技术位置：首页或公司介绍页的 `application/ld+json`。
- 完成条件：使用稳定的 Organization `@id`；所有语言指向同一企业实体；通过 Schema.org/Google 验证工具；不承诺一定产生知识面板或增强结果。

#### [ ] T-05 决定是否使用 LocalBusiness JSON-LD

- 需要资料：完整大阪地址与邮编、公开电话、营业时间、服务区域、地图坐标，以及是否真实接待客户。
- 决策规则：如果只是法定注册地址、不接待客户，优先使用 `Organization.address`，不应为了 SEO 强行标记为 LocalBusiness。
- 页面要求：结构化数据中的地址和联系方式应与公司介绍或联系页面可见内容一致。
- 完成条件：产品经理确认业务类型和地点用途后再实施；不得推测或虚构营业信息。

#### [ ] T-06 决定是否使用 OfferCatalog

- 前置依赖：Sprint 2 完成卡片真实操作和对应详情页/目标 URL。
- 需要资料：产品与服务正式分类、名称、准确说明、详情 URL；只有真实存在时才提供价格、币种、库存或询价状态。
- 当前判断：没有详情页和真实交易信息时，不应虚构 Offer；该标记也不能保证进入 AI 搜索答案或特殊搜索卡片。
- 完成条件：先确认目录模型确实描述现有业务，再添加并通过结构化数据验证。

#### [ ] T-07 建立 Metadata 与结构化数据验收记录

- 自动检查：HTML metadata、canonical/hreflang 一致性、robots.txt、sitemap、JSON-LD 语法和目标 URL 状态。
- 外部检查：目标分享平台调试器、Google Rich Results Test、Search Console URL Inspection。
- 人工检查：分享图构图与语言、公开公司资料准确性、Logo 使用、平台实际预览，以及结构化数据与页面可见内容的一致性。
- 完成条件：保存验证日期、环境、URL、结果和已知平台缓存限制；自动化结果不能替代产品经理对品牌内容和公司事实的批准。
