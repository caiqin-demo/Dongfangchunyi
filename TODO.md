# 项目测评遗留 TODO

本文档记录 Landing Page 与 Sprint 2 三个产品页面历次第三方测评中尚未完成、需要产品经理确认或需要进一步讨论的事项。测评意见是待验证的输入，不是自动修改授权；实施前仍须遵守 `AGENTS.md` 的验证期变更控制规则。

已完成的修复不重复列入。已由产品明确确认保留的设计也不列为缺陷，例如三张产品卡片继续使用相同产品图。

## Hero

### [ ] H-01 确认 Hero 高度与首屏构图目标

- 状态：需要产品经理讨论。
- 测评意见：当前 Hero 基准高度为 `474px`，测评建议参考更高的美式营销站首屏。
- 当前判断：不存在适用于所有美国网站的 `600–800px` 或 `85vh` 强制标准；是否增高属于品牌构图与首屏信息密度决定，不应仅凭行业概括修改。
- 需要确认：桌面、窄桌面和移动端的批准参考图；首屏是否需要露出下一节；Hero 图片的焦点和安全裁切区域。
- 完成条件：产品经理批准各关键视口的目标构图，并在所有支持语言下验证标题、说明和 CTA 无裁切、重叠或横向溢出。

### [ ] H-02 建立 CTA 转化目标和验证方式

- 状态：需要产品经理与数据负责人讨论。
- 已完成：CTA 的最小高度、字号、宽度、键盘访问和目标锚点已经修复并验证。
- 未完成：不能仅凭视觉测评证明 CTA “不转化”。
- 需要确认：主要转化目标、CTA 文案、最终目标页面或表单、事件名称、基准转化率，以及是否进行 A/B 测试。
- 完成条件：CTA 文案和去向得到产品批准；如要求验证转化效果，必须有可审查的分析事件和实验口径。

## Visual system

### [ ] V-01 在 Sprint 2 实现六张卡片底部操作条

- 目标 Sprint：Sprint 2。
- 状态：功能已规划，当前视觉设计必须保留。
- 当前情况：产品和服务卡片底部的 `+` 操作条是后续功能入口，不是待删除的假按钮。
- 需要确认：整张卡片还是操作条可点击；目标详情页、弹层或展开内容；每种语言的可访问名称；打开后的返回和焦点行为。
- 完成条件：鼠标、键盘和辅助技术均可操作；存在清晰的 hover、focus 和 active 状态；无障碍名称描述真实动作；不再使用纯装饰语义。

### [ ] V-02 评估是否引入独立拉丁展示字体

- 状态：低优先级设计讨论。
- 测评意见：`ABOUT US`、`Yeast Two Hybrid` 等拉丁文字目前使用 CJK 字体的拉丁字形，品牌个性较弱。
- 需要确认：品牌是否需要独立拉丁字体；批准字体、字重、许可和使用范围；增加字体后的性能预算。
- 完成条件：只有在产品经理批准明确的排版目标后实施；验证所有支持语言的字体回退、网络请求和布局偏移。

### [ ] V-03 复核核心卡片右上角装饰的可见性

- 状态：需要视觉确认。
- 测评意见：`.core-card::before` 与卡片背景亮度接近，装饰可能难以察觉。
- 需要确认：该装饰是否属于已批准设计；应提高对比度、保持弱化效果，还是移除。
- 完成条件：基于批准的设计基准决定，不得仅凭测评自动删除；在常用显示亮度和中、深色显示环境下人工验收。

### [ ] V-04 决定是否需要页面进入或滚动动效

- 状态：低优先级产品讨论。
- 当前判断：“没有动效”不是缺陷，不能为了显得专业强制添加动画。
- 需要确认：动效需要表达的状态、层级或信息关系；允许的持续时间和性能预算。
- 完成条件：只有存在明确交互目的时实施；所有非必要动效必须尊重 `prefers-reduced-motion`，并避免影响阅读、性能和截图回归稳定性。

## Layout

### [ ] L-01 确认全宽区块之间的分隔方式

- 状态：需要产品经理讨论。
- 测评意见：当前 `0.5rem` 面板间距可能被理解为渲染接缝。
- 需要确认：采用无缝全宽区块，还是明确的卡片式分区；若使用卡片式分区，需要批准外边距、圆角、背景和阴影关系。
- 完成条件：桌面与移动端使用同一可解释的分区逻辑，不出现像意外白缝或断层的视觉效果。

### [ ] L-02 逐步收敛剩余任意尺寸值

- 状态：技术债，需要视觉基准后实施。
- 已完成：主要容器、面板最小高度和部分布局值已经抽取为共享 token 或复用类。
- 未完成：页面仍有若干用于复刻已批准设计的任意字号、间距和尺寸值。
- 需要确认：哪些像素值属于必须保持的设计基准，哪些可以映射到统一的 4px/8px 间距阶梯。
- 完成条件：优先使用语义 token 和 Tailwind 尺度；任何会改变已批准视觉的收敛必须经过截图对比和产品审查。

### [ ] L-03 评估移动优先断点迁移

- 状态：技术债，需要单独实施计划。
- 测评意见：当前布局仍主要使用 `max-[960px]`、`max-[799px]` 和 `max-[640px]`，不是完整的移动优先标准断点体系。
- 风险区间：641–959px 的平板和窄桌面视口需要持续重点验证。
- 完成条件：若迁移，必须先固定现有视觉基准；覆盖每个断点两侧及所有支持语言，不得为追求标准类名而引入回归。

### [ ] L-04 决定移动导航的长期模式

- 状态：需要产品经理与可访问性讨论。
- 当前情况：移动端仍显示五个横向导航项；文字变长或新增语言时可能拥挤，点击目标和换行风险会增加。
- 需要确认：继续保留横向导航，还是采用菜单按钮；若采用菜单，需确定抽屉、弹层或下拉形式。
- 完成条件：所有支持语言在最窄目标视口可用；点击目标、键盘、焦点管理、Escape 和可访问名称通过验收。若实现交互式移动菜单，按 `AGENTS.md` 优先使用 Headless UI。

## Dead code from a previous life

### [ ] D-01 在 Sprint 2 清理 `card-action` 的临时状态

- 目标 Sprint：Sprint 2。
- 状态：等待 V-01 卡片功能实现。
- 已完成：旧版无样式支持的语义 class 已清理；`.core-card` 是实际 CSS 选择器。
- 未完成：`card-action` 当前保留用于计划中的卡片操作条，但尚无真实交互。
- 完成条件：Sprint 2 将其转为正式交互组件或明确的测试钩子，并补充注释/测试；不得在功能完成前误删。

## Sprint 2 — 三个产品页面验收遗留

适用页面：`products/antibody-products`、`products/elisa-kits` 和 `products/lab-instruments` 的中文与日文版本。

已完成的断点 token、Hero/正文容器对齐、抗体卡片 subgrid、规格卡 container query、500px 单列修复、矩阵表格语义与原生 sticky、ELISA 图表响应式资源、首页 LCP、字体请求隔离、本地化 404，以及已删除的 `overview` / `pipette.sectionTitle` 不再列入待办。

### [ ] P-01 决定 ELISA 五联手册在笔记本与移动端的布局方式

- 状态：需要产品经理确认；当前实现可用，但布局策略尚未形成批准基准。
- 优先级：中。
- 已验证事实：`ElisaManualSeries` 使用 `min-w-[81.25rem]` 保持五栏同排，并放在可聚焦的横向滚动区域中；因此窄于约 1300px 的内容区会出现组件内部横向滚动。各栏仍用 `min-h-28` 和 `min-h-24` 对齐标题与文案带。
- 当前判断：横向滚动现在是明确且可键盘聚焦的组件行为，不再是页面级意外溢出；但“始终保持五联同排”属于产品信息呈现决定。固定最小高度不会直接裁切内容，却仍是依赖内容长度的人工对齐方法，翻译增长时可能失去一致性。
- 需要用户与产品经理确认：五个手册面板是否必须保持一个连续横向序列；桌面笔记本是否允许横向滚动；移动端应保持滚动、改为单列/多行，还是提供分页/切换；五栏标题、规格和图表是否必须横向对齐；允许的中日文高度差异。
- 获批后技术范围：若保留同排滚动，记录批准的滚动提示、焦点行为和最小宽度；若允许响应式重排，优先使用内容驱动高度或 subgrid，移除仅用于对齐的 `min-h-28` / `min-h-24`，不得缩小某一语言文字或裁切科学图表。
- 完成条件：在中文和日文的 390、641、700、800、950、1440px 下无页面级横向溢出；组件滚动或重排符合批准方案；标题、规格、图表阅读顺序和键盘操作正确。

### [ ] P-02 评估实验仪器透明产品图的响应式派生资源

- 状态：需要先测量，再由产品经理批准是否创建派生图；不得覆盖现有原图。
- 优先级：中低。
- 已验证事实：`pipette-core-transparent.webp` 约 342KB，`curling-vortex-mixer-transparent.webp` 约 333KB；前者在页面中最大约 208 CSS px，后者会随卡片宽度变化。当前没有针对这些展示宽度提供独立 `srcset` 派生图。
- 当前判断：文件偏大这一事实成立，但不能只根据文件大小自动重编码。它们是产品外观图，必须先验证透明度、边缘、颜色和实际浏览器尺寸；原始资源应继续保留。
- 需要用户与产品经理确认：这些图片是否属于必须原样保存的客户/品牌资产；是否批准生成不覆盖原文件的 WebP/AVIF 尺寸派生图；允许的视觉差异和质量标准；需要支持的最大展示宽度与高密度屏幕倍数。
- 获批后技术范围：依据真实渲染宽度生成少量有意义的派生尺寸，使用 `next/image` 的 `sizes`/响应式候选提供；不得重画、裁切、改色、改变透明背景或删除原文件。
- 完成条件：生产浏览器只请求接近展示尺寸的候选资源；透明度、比例和产品细节与原图人工对比通过；中日文在移动端与桌面端均无裁切或布局偏移。

### [ ] P-03 增加本地化运行时错误边界

- 状态：工程缺口，需要产品经理批准错误状态内容；`loading.tsx` 不在本项范围。
- 优先级：中低。
- 已验证事实：本地化 404 已实现，但 `/zh`、`/ja` 下仍没有 `error.tsx`；产品页发生未预期渲染错误时，没有品牌化、本地化的恢复界面。三个产品页当前为静态页面，没有异步数据等待，因此缺少 `loading.tsx` 本身不是缺陷。
- 需要用户与产品经理确认：中文和日文错误标题、说明、重试按钮、返回首页/产品区入口；是否显示联系邮箱；哪些技术信息禁止展示给访客；视觉上是否复用 404 页面框架。
- 技术范围：错误边界必须是最小 Client Component，仅承担捕获错误和 `reset()` 重试；不得把产品页主体改成 Client Component；日志或监控接入需另行批准供应商与隐私范围。
- 完成条件：两种语言均能显示正确错误文案；重试和返回链接可通过键盘操作；不泄露堆栈、路径或用户数据；正常静态页面的客户端 JavaScript 不因该改动无必要扩大。

### [ ] P-04 确认全局 404 的默认语言与实验性能力退出条件

- 状态：上线前技术复核，并需要产品经理确认未知语言路径的用户体验。
- 优先级：中。
- 已验证事实：`/zh/*`、`/ja/*` 的未知路径会返回对应语言的品牌化 404；完全未知路径使用默认日文 404。由于项目采用中文和日文两个根布局，当前全局兜底使用 Next.js 16.3.1 的 `experimental.globalNotFound`。
- 当前判断：当前行为已通过 HTTP 404、`noindex` 和本地化内容检查，但实验性框架能力需要在 Next.js 升级和上线前重新验证，不能默认视为长期稳定 API。
- 需要用户与产品经理确认：无语言前缀或不支持语言（例如 `/en/...`）应显示默认日文、语言选择界面还是跳转到某一首页；404 是否需要保留用户输入路径；是否需要在未知语言场景提供中文入口。
- 技术范围：在升级 Next.js 或生产发布前核对官方稳定状态与构建行为；若能力仍为实验性，评估保持现状、调整路由架构或采用其他可返回真实 404 的方案，不得以返回 200 的普通页面伪装 404。
- 完成条件：产品经理批准默认语言策略；生产环境的未知路径返回 404 和 `noindex`；最终 DOM 语言、标题、恢复链接及中日文字体正确；退出实验能力的触发版本或复核日期有记录。

### [ ] P-05 建立 Sprint 2 产品页人工验收记录

- 状态：本阶段不建设 CI，以可重复的人工验收记录作为交付门槛。
- 优先级：中。
- 已验证事实：仓库规则要求对相关多语言 UI 变更检查 390、641、700、800、950、1440px，但目前没有针对三个产品页的统一验收记录模板和签字结果。
- 当前判断：截图本身不能证明计算样式、语义、键盘和溢出正确；在 CI 明确暂缓期间，应先把必要的人工检查变成可执行清单，而不是声称所有规则已经自动化。
- 需要用户与产品经理确认：Sprint 2 的验收人；支持浏览器与最低版本；哪些视口必须保存截图；科学图表、产品图片、产品事实和翻译分别由谁签字；允许延期的问题及重新检查日期。
- 记录范围：三条产品路径 × 中文/日文；断点两侧；页面级和组件级横向滚动；表格首列/表头固定；键盘焦点；200%/400% 缩放；标题层级；图片比例与 alt；计算字体角色一致性；关键文本与焦点对比度；控制台和网络错误。
- 完成条件：仓库中保存不含本机路径或个人信息的验收模板/结果；每项注明日期、浏览器、页面、视口、通过/失败、证据和负责人；失败项链接回明确 TODO，不用“视觉正常”替代测量结果。

### [ ] P-06 为抗体产品固定四项 tuple 定义扩展触发条件

- 状态：当前无需重构；在增加第五个抗体产品或确认大规模目录前由产品经理复核。
- 优先级：低。
- 已验证事实：抗体内容目前用固定四项 tuple 和产品 ID 联合类型，同时在两种语言、产品顺序、实验图片及 availability 数据中保持穷尽映射。它能在编译期发现漏翻译、漏图片和漏库存映射，但新增产品需要同步修改多个位置。
- 当前判断：四个产品、两个语言的规模下，这个权衡合理，不构成缺陷；产品数量达到某个值也不会自动要求引入 Sanity/Payload。CMS 决策仍受 T-09 和 T-11 的编辑责任、审批、预览、成本与运维要求约束。
- 需要用户与产品经理确认：下一阶段预计增加的产品数量和时间；目录由开发人员还是非开发人员维护；是否需要排序、上下架、草稿、审批和预览；产品图片、规格与库存数据的权威来源和审核人。
- 触发条件：增加第五个抗体产品、产品更新开始频繁出错，或产品经理确认将扩展为持续维护的大目录时，先评估集中 ID/顺序和穷尽映射，之后才根据 T-09/T-11 决定是否需要外部 CMS。
- 完成条件：记录继续使用固定 tuple 或迁移数据模型的书面决定、触发日期和负责人；任何迁移必须保持双语条目、图片和 availability 的编译期或 schema 完整性检查。

### [ ] P-07 定义 ELISA 目录实体、URL 粒度与搜索筛选范围

- 状态：信息架构产品决策；在获得权威产品资料前不得批量生成详情页。
- 优先级：高。
- 已验证事实：当前 ELISA 分类页在一个 URL 中输出 151 个靶点 × 10 个物种列；中文生产 HTML 约 753KB，日文约 770KB。仓库没有逐靶点或逐 SKU 路由、目录搜索、物种/靶点筛选、分页或可分享的筛选 URL。
- 当前判断：缺少可独立发现的目录实体和检索入口是事实，也会限制精确搜索词对应的页面标题与内容；但不能从竞品做法直接推出“151 × 10 个组合都应生成页面”。如果页面只有名称和一个勾选，会形成重复或信息不足的薄页面，也无法可靠满足科研采购查询。
- 需要用户与产品经理确认：业务上的最小产品实体是靶点、靶点 × 物种、包装 SKU，还是厂商货号；哪些组合真实销售；每项是否有独立名称、货号、灵敏度、检测范围、样本类型、方法、说明书和询价状态；首期应覆盖全部目录还是优先靶点；搜索与筛选是否属于 Sprint 3、上线阻断项或后续增强。
- 需要确认 URL 与语言策略：批准的英文 slug 来源和重名规则；中文/日文是否共用同一稳定 slug；无效或下架产品的 404/重定向策略；筛选参数是否可索引；canonical、hreflang、sitemap 和内部链接如何保持一致。
- 建议阶段：先建立权威目录 schema 和数据完整性报告；再实现客户端或服务端搜索/筛选改善当前目录可用性；只有资料完整且产品经理批准的实体才生成独立静态路由，并逐批验证搜索收录与维护成本。
- 完成条件：产品经理签署实体关系、首期范围、URL 规则、权威数据源和负责人；目录可按批准字段检索；独立页面拥有真实差异化内容、本地化 metadata、正确状态码和互相一致的 canonical/hreflang/sitemap，不自动制造空白或重复页面。

### [ ] P-08 为可索引产品页定义 Product 与 BreadcrumbList 结构化数据

- 状态：依赖 P-07 的产品实体和 URL 决策；与 Sprint 5 的 T-02～T-07 协同，不重复实施 Organization、Open Graph 或 robots。
- 优先级：中高，实施 Sprint 由产品经理确定。
- 已验证事实：仓库当前没有 `application/ld+json`；T-04 已记录 Organization，T-06 已记录 OfferCatalog，但尚未明确记录逐产品 `Product` 和可见面包屑对应的 `BreadcrumbList`。
- 当前判断：结构化数据缺失是事实，但 JSON-LD 只能描述页面上真实可见、可验证的数据，不能替代独立产品内容，也不能保证 rich result。没有真实价格、币种和在线库存时，不应虚构 `Offer`；没有 GTIN/MPN 时也不应猜测。
- 需要用户与产品经理确认：哪些实体可声明为 `Product`；批准的品牌、制造商、SKU/货号、MPN/GTIN、图片、描述、适用物种和公开供应状态；是否存在可公开价格；询价商品是否只描述产品而不输出 `Offer`；面包屑分类名称和层级。
- 技术范围：从同一类型化产品数据生成页面可见内容、metadata 和 JSON-LD，避免三套事实漂移；`BreadcrumbList` 必须对应页面上真实可见、可点击的面包屑；继续由 T-02/T-03/T-04 管理 OG、robots 和 Organization。
- 完成条件：Schema.org/目标搜索验证工具无事实性错误；JSON-LD 与页面文案、URL、图片和库存/价格状态一致；中日文实体关联明确；产品经理或内容负责人签署所有业务字段，且验收记录纳入 T-07。

### [ ] P-09 定义携带产品上下文的询价与归因流程

- 状态：产品、销售运营和隐私决策；复用 T-13 联系流程与 T-14 Analytics，不预先指定 HubSpot、Salesforce 或 GA4。
- 优先级：高，是否属于首发阻断项由产品经理确认。
- 已验证事实：三个产品页当前只提供共享 `mailto:` 联系入口；没有站内询价表单、SKU/靶点上下文传递、CRM/邮件提交端点或 Analytics。`mailto:` 可作为低成本备用入口，但依赖访客本地邮件配置，无法提供可靠的站内成功状态和归因数据，公开邮箱也会增加被采集风险。
- 需要用户与产品经理确认：主要动作是“询价”“索取报价”“联系销售”还是“索取资料”；入口位于分类页、详情页、每行/SKU 还是固定联系区；必须携带的产品 ID、靶点、物种、包装和语言；允许访客修改哪些上下文字段；`mailto:` 是否保留为备用方式。
- 需要销售运营确认：请求进入邮箱、CRM、数据库还是工单；HubSpot/Salesforce/其他供应商账号、费用、字段映射、负责人、通知、SLA、重复线索和失败回退；任何平台名称都不是测评自动授权。
- 需要隐私与数据确认：表单字段、必填项、同意文案、保存期限、删除流程、反垃圾与速率限制；允许记录的归因字段；分析事件不得发送姓名、邮箱、电话、自由文本或其他个人数据。
- 技术范围：具体表单、安全、交付和错误处理由 T-13 管理；事件平台、环境、去重和隐私由 T-14 管理。本项负责产品上下文、入口位置和跨页面转化路径，避免实现一个与目录脱节的通用联系表单。
- 完成条件：从批准的产品/SKU 入口提交后，目标系统收到稳定产品 ID 和必要上下文；访客获得可访问的成功/失败反馈；失败时有批准的备用联系方式；产品经理能按批准口径查看不含敏感数据的转化结果。

### [ ] P-10 确认 Datasheet、CoA 与 Manual 的资料下载策略

- 状态：需要产品经理、产品资料负责人和供应商确认文件是否存在及是否允许公开；当前仓库没有 PDF/Word 下载资料。
- 优先级：中高。
- 当前判断：实验室产品用户通常需要规格书、说明书或批次证书，但“行业常见”不能证明本项目已拥有、获授权或应该公开这些文件。Datasheet、Manual 和 CoA 也不是同一种内容：CoA 往往与批次相关，不能用一份静态通用 PDF 代替。
- 需要用户与产品经理确认：每个产品实际有哪些资料；权威来源、版权/分发授权、语言、版本、发布日期、适用 SKU 和更新负责人；哪些文件公开下载，哪些需要询价或按批号查询；过期版本如何撤回和重定向。
- 需要内容与可访问性确认：文件名和页面链接文案；PDF 是否包含可检索文本、正确标题/语言/阅读顺序、表格语义和替代文本；科学图表和规格是否必须保持原始文件不变。
- 技术范围：只有收到批准的原始文件后才建立稳定下载 URL、文件 metadata 和产品关联；不得由页面现有摘要反向编造 datasheet/CoA；如需要受控下载、表单前置或批号查询，应另行批准权限、存储和隐私方案。
- 完成条件：页面明确显示文件类型、语言、版本/日期和适用产品；下载 URL 稳定并返回正确 MIME/type 与文件名；旧版本策略有效；人工核对文件内容与页面规格一致；若 T-14 获批，可记录不含个人数据的下载事件。

### [ ] P-11 定义面包屑、关联产品与跨产品线推荐规则

- 状态：产品信息架构与销售策略决策；依赖 P-07 的目录层级。
- 优先级：中。
- 已验证事实：产品页 Header 只有返回产品区的链接，Footer 提供三条产品线导航；页面没有可见面包屑、详情层级、关联产品模块或基于当前产品的交叉推荐。
- 当前判断：可见面包屑在建立更深产品路由后有明确导航价值；当前只有一级分类页时，返回链接已提供基本回退，立即增加“首页 › 产品 › 当前页”是否值得属于设计决定。关联产品和 cross-sell 必须来自批准的业务关系，不能依据名称或数组位置自动推断。
- 需要用户与产品经理确认：批准的目录层级和面包屑文案；面包屑是否从分类页开始还是仅用于详情页；哪些产品可以作为替代品、配套品、耗材或跨产品线推荐；排序、最大数量、双语说明和无相关推荐时的表现；推荐关系由谁维护和审核。
- 技术范围：面包屑使用原生导航与真实链接，之后才生成对应 `BreadcrumbList`；关联产品使用稳定产品 ID 和显式关系类型，不能把 Footer 通用导航包装成个性化推荐，也不能在无数据时展示占位产品。
- 完成条件：键盘和辅助技术能理解面包屑当前位置；所有链接保持当前语言并返回 200；推荐内容与批准关系一致、无下架或循环错误；中文和日文的层级、数量与目标 URL 对齐。

### 本轮明确不新增的待办

- 不新增 `loading.tsx`：当前产品页静态生成，没有已验证的加载状态缺陷。
- 不引入 `clsx`/`cva`：它们不解决 Tailwind utility 冲突，当前也没有已证实的冲突；如未来需要合并冲突类，应针对真实组件 API 重新评估。
- 不新增图片全局配置或 bundle analyzer：当前没有证据证明 Next.js 默认 `deviceSizes`/格式配置导致缺陷，bundle analyzer 也不是 Sprint 2 的必需功能。
- 不重复添加 `robots.ts`：已由 T-03 记录在 Sprint 5。
- 不把 Playwright、axe 或 Lighthouse CI 加入 Sprint 2：用户已明确当前阶段不建设基础 CI，未来范围继续由 T-16 管理。

## Technology

### [ ] T-01 澄清并修复默认生产构建门槛

- 状态：高优先级工程前置事项。
- 当前情况：`npm run build` 使用 Turbopack 时在受限环境出现 PostCSS/Tailwind 内部进程绑定端口 `Operation not permitted`；`next build --webpack` 可以通过。
- 需要确认：该问题是否只存在于本地沙箱，还是会在正式 CI/Vercel 环境复现；项目是否继续以默认 Turbopack 构建作为必过门槛。
- 完成条件：默认 `npm run build` 在正式验收环境稳定通过，或由团队批准、记录并在 CI 中采用明确的替代生产构建命令。

### Content model

#### [x] T-08 完成第一阶段类型化内容模型重构

- 已完成：将首页内容从页面组件拆分为按语言维护的 TypeScript 内容文件；使用命名字段代替位置元组；为导航、产品、服务和介绍条目增加稳定 ID；服务图标改为按稳定 ID 映射，不再依赖数组下标。
- 约束：本阶段没有修改已批准文案、视觉设计或交互，没有安装依赖，也没有引入外部内容服务。
- 自动保证：TypeScript 可检查不同语言的字段形状、条目 ID 和必要字段是否一致，并在错误映射或漏项时阻止类型检查通过。
- 不能自动保证：翻译是否准确、文案是否符合品牌语气、业务事实是否正确，仍必须由产品经理或内容负责人确认。

#### [ ] T-09 确认内容编辑责任与发布流程

- 状态：等待产品经理决定是否做、何时做。
- 需要确认：谁负责编辑和审核；修改频率；是否允许非开发人员直接编辑；是否需要草稿、预览、定时发布和审批；内容发布是否必须立即生效，还是可以随代码部署发布。
- 还需确认：未来是否会增加新闻、案例、产品详情、长篇文章或更多语言；是否接受外部服务、账号、月费、权限管理和运维责任。
- 建议：低频、由开发人员维护的首页文案继续使用当前类型化 TypeScript；产品经理提供文案、开发人员复核发布时，可采用仓库内文件配合 PR 审查。
- 完成条件：产品经理书面确认编辑者、审核者、发布步骤、响应时间和回滚责任，之后再选择技术方案。

#### [ ] T-10 决定是否采用 JSON 或 MDX 内容文件

- 状态：待讨论；当前类型化 TypeScript 已满足第一阶段，不需要立即迁移。
- JSON 适用条件：内容仍随代码版本管理，但希望内容与组件更明显分离；需要额外的 schema/类型生成或运行时校验，避免失去当前 TypeScript 的直接检查。
- MDX 适用条件：出现案例、新闻、产品详情、博客等需要标题层级、图片、表格或嵌入组件的长篇内容；不建议为了导航、按钮和短卡片文案把整个首页迁移到 MDX。
- 与 `AGENTS.md` 的关系：新增 MDX 或校验依赖必须由已批准的产品需求证明必要性，不能预先安装；无论使用 JSON 还是 MDX，通常仍需 PR、构建和部署，并不会自动提供浏览器后台编辑。
- 决策建议：保留当前 TypeScript 作为短结构化文案的默认方案；只有明确出现长篇编辑需求后再评估 MDX。

#### [ ] T-11 决定是否引入 Sanity 或其他轻量 CMS

- 状态：待产品经理决定是否做、何时做；本阶段不安装、不接入。
- 适用条件：非开发人员需要在浏览器中编辑、预览、审批或定时发布，并且不希望每次文案修改都经过代码提交和部署。
- 需要讨论：供应商与费用、账号和角色、数据所有权与导出、隐私和可用性、API token 与环境变量、CORS、草稿预览、缓存/重新验证、静态生成策略、故障回退、供应商锁定和现有内容迁移。
- 与 `AGENTS.md` 的关系：CMS 会引入外部服务、配置和可能的依赖，超出当前已批准需求；在产品经理批准上述责任和预算前不得安装或实施。
- 决策建议：只有当 T-09 确认存在持续的非开发人员自助发布需求时，才制作候选 CMS 对比和实施方案；否则继续使用仓库内类型化内容。

#### [ ] T-12 处理 Contentlayer 测评建议

- 状态：不采用，除非产品经理要求重新调研维护中的替代方案。
- 当前判断：原项目公开 issue 显示 `next-contentlayer@0.3.4` 的 peer dependency 仍只覆盖 Next.js 12/13，Next.js 14/15 兼容请求长期未落实；本仓库使用 Next.js 16，因此不适合作为新的生产内容基础设施直接引入。
- 需要讨论：若未来需要本地内容流水线，应重新核实当时仍在维护、兼容当前 Next.js 版本的方案，并比较迁移成本、类型安全和长期维护风险。
- 与 `AGENTS.md` 的关系：不得因为第三方测评列出工具名称就安装依赖；替代工具同样需要明确产品需求、兼容性验证和产品经理批准。
- 完成条件：产品经理决定无需本地内容流水线，或批准一项经过重新调研的替代方案及实施 Sprint。

### Missing infrastructure — Sprint 1 评估

当前项目仍处于 Landing Page 初始开发阶段，其他页面、联系流程和运营基础设施尚未开始。以下“缺少”项是未来能力清单，不自动构成 Sprint 1 缺陷，也不代表测评指定的供应商或技术组合已经获得实施批准。

#### [ ] T-13 确认联系入口、表单范围和交付流程

- 状态：等待用户与产品经理决定是否做、何时做；如果“访客可提交询盘”被定义为上线阻断项，则提升为高优先级/P0，否则进入后续 Sprint。
- 测评意见：项目没有表单栈；建议使用 Server Action、验证、Turnstile/hCaptcha 和 Resend。
- 当前事实：导航中的“联系我们”目前只定位到 Footer；仓库没有联系表单、提交端点、服务端验证、反垃圾机制、邮件或 CRM 交付流程。
- 当前判断：缺少联系提交能力的事实成立，但测评指定的组合不是唯一方案。Server Action 是当前 App Router 原生表单的优先候选，不是强制；服务端验证和滥用防护是上线表单的必要责任，但验证码与邮件供应商必须根据实际流程选择；若询盘进入 CRM、数据库或工单系统，则不一定需要 Resend。
- 需要确认产品范围：Sprint 1 是否只需要 Landing Page 展示，还是首发必须能够提交询盘；入口位于 Footer、独立联系页、弹层还是其他位置；目标上线 Sprint 和是否属于 P0。
- 需要确认字段与内容：每种语言的标题、说明、字段、必填项、错误提示、成功提示和自动回复；是否允许附件；是否需要公司、电话、主题、同意条款等字段。
- 需要确认交付与责任：询盘发送到邮箱、CRM、数据库还是工单系统；正式收件人和负责人；通知、回复时限、失败重试、备用联系方式、数据保存期限和删除流程。
- 需要确认安全与隐私：服务端 schema 验证、速率限制、蜜罐字段和验证码是否组合使用；Turnstile、hCaptcha 或其他方案的账号、费用、可用地区、数据处理和故障回退；隐私政策、用户同意与日志中不得保存的字段。
- 需要确认邮件方案：只有确定以邮件交付时，再比较 Resend 或其他供应商；确认发信域名、DNS 验证、发件人、收件人、密钥负责人、配额、退信处理和测试环境，任何凭据只通过环境变量管理。
- 与 `AGENTS.md` 的关系：在产品范围和供应商未批准前不得预先安装表单、验证码或邮件依赖。若实现，字段级错误必须持续可见，不能只用 toast；临时提交结果可按规则使用 React Hot Toast。
- 自动验收：有效/无效/重复/超限请求，服务端校验，失败与超时处理，防重复提交，反垃圾验证，邮件或目标系统交付，以及日志不泄露敏感字段。
- 人工验收：所有支持语言的文案、隐私说明、键盘与屏幕阅读器流程、移动端输入体验、真实收件和业务回复流程。
- 完成条件：产品经理批准范围、字段、供应商、隐私和运营责任；实现后保存端到端测试记录和真实交付验收证据。

#### [ ] T-14 定义 Analytics 目标、事件和隐私边界

- 状态：等待用户、产品经理和隐私/数据负责人决定是否做、何时做；建议在正式推广或开始评估 CTA 转化之前完成，当前优先级为中。
- 测评意见：项目没有 Analytics，无法知道访客是否到达产品区域；建议接入 Vercel Analytics。
- 当前事实：仓库没有分析依赖、脚本或事件；目前无法从产品数据判断访客是否看到核心产品、点击 CTA、切换语言或开始联系流程。
- 当前判断：建立分析能力的建议成立，但 Vercel Web Analytics 的默认页面浏览统计不能自动证明访客滚动到 `#products`；区块曝光需要单独的可见性事件、阈值和去重规则。“一行代码”和“对日本/EU天然合规”都是过度简化，仍涉及控制台启用、依赖、部署、套餐、自定义事件和隐私审查。
- 需要确认业务问题：希望回答页面访问、核心产品/服务区块曝光、CTA 点击、语言切换、卡片点击、联系表单开始/成功/失败中的哪些问题；每项指标由谁查看、如何影响产品决策。
- 需要确认事件口径：事件名称、稳定内容 ID、语言、页面、触发条件；区块达到多少可见比例、持续多久才算曝光；每次页面、每次会话还是每位访客只记录一次；机器人、开发和预览流量如何排除。
- 需要确认平台与预算：使用 Vercel Web Analytics、其他平台或暂不接入；Vercel 项目和套餐是否支持所需自定义事件；事件额度、数据保留期、导出需求、仪表板访问权限和费用负责人。
- 需要确认隐私：适用地区和内部合规要求；是否需要同意管理；隐私政策如何披露；允许采集的 URL、查询参数、地理和设备信息；自定义事件禁止发送姓名、邮箱、电话、自由文本、token 或其他可识别信息。
- 需要确认环境：只采集生产环境还是包含预览；本地开发必须默认不发送正式数据；如何验证事件而不污染生产报表。
- 与 `AGENTS.md` 的关系：分析依赖和外部数据服务需在指标、供应商、预算及隐私边界批准后才能安装；事件身份应使用内容模型中的稳定 ID，不得使用翻译文案或数组位置。
- 自动验收：页面浏览和批准的自定义事件只在目标环境发送；事件名称、字段、去重和语言值正确；无敏感数据；脚本失败不阻塞页面功能。
- 人工验收：仪表板能回答已批准业务问题；产品经理理解数据口径、抽样/拦截限制和不能由数据直接证明的结论。
- 完成条件：批准事件字典、供应商、套餐、隐私说明、访问权限和负责人；部署后保存浏览器网络请求与仪表板收数证据。

#### [x] T-15 确认 `tsconfig.tsbuildinfo` 未进入版本控制

- 测评意见：仓库根目录的 `tsconfig.tsbuildinfo` 已提交，应该加入 `.gitignore`。
- 复核结果：该文件是 TypeScript 增量检查生成的本地缓存，目前约 113KB；`.gitignore` 已包含 `*.tsbuildinfo`，Git 未跟踪该文件。
- 当前判断：测评结论已过时或基于旧状态，不需要删除源码、修改 `.gitignore` 或执行 `git rm --cached`。
- 后续检查：若将来 `git ls-files tsconfig.tsbuildinfo` 出现结果，才需要先确认历史原因，再将其从 Git 索引移除；不能通过更宽泛的忽略规则掩盖源码或必要配置。
- 完成条件：当前已满足；保持该文件为本地生成物，不提交到仓库。

#### [ ] T-16 建立首期 CI 与 Playwright smoke 门槛

- 状态：仓库确实没有 CI、Vitest 或 Playwright 配置；用户已明确 Sprint 2 暂不建设基础 CI，本项保留为后续规划，目标 Sprint 仍需由用户与产品经理确定。
- 测评意见：PR 至少运行生产构建和一个 Playwright smoke test。
- 当前判断：方向正确，但“一个下午”不是可审查的工作量承诺。首期还需处理依赖安装、浏览器版本、字体和服务器启动、缓存、失败产物、分支保护，以及当前默认 Turbopack 构建门槛。
- 前置事项：先完成或正式决策 T-01；不能在 CI 中无说明地用 `next build --webpack` 永久掩盖默认 `npm run build` 的问题。
- 需要确认 CI 平台：代码托管和执行平台、目标分支、PR/合并/定时触发方式、可用分钟数和账单负责人；是否同时使用 Vercel Preview Deployment。
- 需要确认运行环境：Node 与 npm 固定版本、锁文件安装命令、Linux runner、必要环境变量、生产域名变量和秘密信息由谁维护；Fork PR 是否允许访问 secrets。
- 需要确认首期命令：建议顺序为依赖安装、`npm run lint`、`npm run typecheck`、批准的 production build、启动生产服务器、Playwright smoke；任何替代构建命令必须有明确期限和责任人。
- 需要确认 smoke 范围：至少覆盖所有支持语言的首页可打开、正确 `<html lang>`、导航和 CTA 目标、核心区块存在、语言切换、无横向溢出和关键控制台错误；首期默认 Chromium，WebKit/Firefox 是否加入后续批次由浏览器支持范围决定。
- 需要确认缓存与产物：npm 下载缓存和 Playwright 浏览器缓存策略；失败时保留截图、trace、控制台错误和 HTML report；保留期限及是否可能包含敏感信息。
- 需要确认分支保护：哪些检查为必过、管理员是否允许绕过、Flaky test 如何隔离、谁可批准 golden screenshot 更新，以及失败时由谁负责修复。
- 与 `AGENTS.md` 的关系：Playwright 等依赖只能在 CI/验收需求正式批准后安装并提交锁文件；CI 必须在主仓库和正式配置上运行，不得以 Codex worktree 结果作为交付证据。
- 自动验收：每个 PR 可重复执行规定命令；失败能阻止合并并提供足够产物定位问题；缓存不能掩盖依赖或构建错误。
- 人工验收：产品经理确认 smoke 覆盖的是实际首发路径；团队验证分支保护、失败处理、权限和成本符合工作流。
- 完成条件：CI workflow、脚本、Playwright smoke、失败产物和分支保护全部启用并通过一次故意失败/恢复演练。

#### [ ] T-17 批准基础设施实施顺序和 Sprint 归属

- 建议顺序：先解决 T-01 默认生产构建门槛；再建立 T-16 的 lint/typecheck/build 基础 CI；并行完成 T-13 联系流程与 T-14 分析指标的产品决策；联系功能获批后实现并加入 smoke；正式推广前接入获批 Analytics；最后根据风险扩展浏览器矩阵和更深测试。
- 需要确认：T-13、T-14、T-16 各自属于哪个 Sprint；哪些是首发阻断项；负责人、预算和批准日期；暂缓项何时重新审查。
- 完成条件：用户与产品经理为每项记录 `Do now`、`Schedule` 或 `Do not do`，并为计划实施项指定 Sprint、负责人和验收人。

### Metadata is nearly empty — Sprint 5

以下事项统一留到 Sprint 5。现有的本地化 title、description、`metadataBase`、canonical、`hreflang`、`x-default` 和多语言 sitemap 已完成，不重复列入。

#### [ ] T-02 添加 Open Graph 与 Twitter 分享元数据

- 需要资料：每种语言的分享标题、摘要、正式 URL，以及批准的分享图；建议分享图为 1200×630。
- 需要讨论：所有语言共用一张无文字图片，还是每种语言使用独立图片；不同平台允许的裁切安全区域。
- 技术位置：各语言页面的 Next.js metadata，最终输出到 HTML `<head>`。
- 完成条件：Slack、LinkedIn、LINE 等目标平台或其调试工具能读取正确的标题、摘要、URL 和图片；平台缓存与裁切差异需记录为外部限制。

#### [ ] T-03 添加 `robots.ts`

- 需要资料：允许抓取的公开路径、需要禁止抓取的路径，以及正式 sitemap URL。
- 建议初始策略：允许抓取公开站点并声明 `https://www.easternpurity.com/sitemap.xml`。
- 技术位置：`src/app/robots.ts`，生成 `/robots.txt`。
- 完成条件：生产环境返回有效 robots.txt；不误阻止语言页面、字体、图片和 sitemap；规则经过产品与部署审查。

#### [ ] T-04 添加 Organization JSON-LD

- 需要资料：法定公司名称、各语言展示名称、正式网站、批准的 Logo URL、公开电话/邮箱/地址、官方社交账号。
- 需要确认：所有字段是否真实、可公开，并与页面可见内容保持一致。
- 技术位置：首页或公司介绍页的 `application/ld+json`。
- 完成条件：使用稳定的 Organization `@id`；所有语言指向同一企业实体；通过 Schema.org/Google 验证工具；不承诺一定产生知识面板或增强结果。

#### [ ] T-05 决定是否使用 LocalBusiness JSON-LD

- 需要资料：完整大阪地址与邮编、公开电话、营业时间、服务区域、地图坐标，以及是否真实接待客户。
- 决策规则：如果只是法定注册地址、不接待客户，优先使用 `Organization.address`，不应为了 SEO 强行标记为 LocalBusiness。
- 页面要求：结构化数据中的地址和联系方式应与公司介绍或联系页面可见内容一致。
- 完成条件：产品经理确认业务类型和地点用途后再实施；不得推测或虚构营业信息。

#### [ ] T-06 决定是否使用 OfferCatalog

- 前置依赖：Sprint 2 完成卡片真实操作和对应详情页/目标 URL。
- 需要资料：产品与服务正式分类、名称、准确说明、详情 URL；只有真实存在时才提供价格、币种、库存或询价状态。
- 当前判断：没有详情页和真实交易信息时，不应虚构 Offer；该标记也不能保证进入 AI 搜索答案或特殊搜索卡片。
- 完成条件：先确认目录模型确实描述现有业务，再添加并通过结构化数据验证。

#### [ ] T-07 建立 Metadata 与结构化数据验收记录

- 自动检查：HTML metadata、canonical/hreflang 一致性、robots.txt、sitemap、JSON-LD 语法和目标 URL 状态。
- 外部检查：目标分享平台调试器、Google Rich Results Test、Search Console URL Inspection。
- 人工检查：分享图构图与语言、公开公司资料准确性、Logo 使用、平台实际预览，以及结构化数据与页面可见内容的一致性。
- 完成条件：保存验证日期、环境、URL、结果和已知平台缓存限制；自动化结果不能替代产品经理对品牌内容和公司事实的批准。
